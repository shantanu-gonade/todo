// Grouping project for :core:* submodules.
// Applying `base` registers the `clean` lifecycle task so that Android Studio's
// "Clean Project" action (which explicitly runs :core:clean) succeeds.
plugins {
    base
}
