// Grouping project for :feature:* submodules.
// Applying `base` registers the `clean` lifecycle task so that Android Studio's
// "Clean Project" action (which explicitly runs :feature:clean) succeeds.
plugins {
    base
}
